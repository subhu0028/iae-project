<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/product.css">
    <link rel="stylesheet" href="/css/forms.css">
</head>
<body>
    <%- include('../includes/navigation.ejs') %>

    <main>
        <form class="product-form" action="<% if(editing) { %>edit-application<% } else { %>add-mentor<% } %>" method="post">
            <!-- 
            <div class="form-control">
                <label for="mentorName">Mentor Name</label>
                <input type="text" name="mentorName" id="title" value="<% if(editing) { %><%= application.mentorName %><% } %>">
            </div> -->
            <div class="form-control">
                <label for="userID">User ID</label>
                <input type="text" name="userID" id="price" value="<% if(editing) { %><%= application.userID %><% } %>">
            </div>

            <% if(editing) { %>
                <input type="hidden" value="<%= application._id %>" name="applicationId">
            <% } %>   
            <button type="submit"><% if(editing) { %>Approve<% } else { %>Add User<% } %></button>
        </form>
    </main>
<%- include('../includes/end.ejs') %>