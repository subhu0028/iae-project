/* const mongoose = require('mongoose');

const Schema = mongoose.Schema;

const featureSchema = new Schema({
    mentorName: {
        type: String,
        required: true
    },
    mentorImage: {
        type: String,
        required: true
    },
    title: {
        type: String,
        required: true,
    },
    content: {
        type: String,
        required: true
    }
});

module.exports = mongoose.model('Mentor', featureSchema); */

